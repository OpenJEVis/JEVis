#!/bin/bash

source ../etc/jevis.env

# Allways start jealarm before the alarming
${JEVIS_HOME}/bin/jecalc.sh

java -DserviceLogFile=/var/log/jealarm.log -XX:+UseG1GC -jar ${JEVIS_HOME}/JEAlarm/target/SimpleAlarm.jar --config ${JEVIS_HOME}/etc/jevis.xml $?



